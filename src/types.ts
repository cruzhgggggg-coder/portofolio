export interface Project {
  id: string;
  title: string;
  category: string;
  year: string;
  description: string;
  image: string;
  status: string;
}

export interface NavItem {
  label: string;
  href: string;
}
